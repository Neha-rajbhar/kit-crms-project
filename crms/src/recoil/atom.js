import { atom } from "recoil";

export const getAttendenceByIds = atom({
  key: "getAttendenceByIds",
  default: [],
});

export const getCurrentUser = atom({
  key: "getCurrentUser",
  default: [],
});
